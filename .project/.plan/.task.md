# Текущие задачи для стабильной версии

- [ ] скрипт проверкти вариантов на значения с единственным классом типа `cover: "object-cover"` так как эти пропы уже есть в таблице `utility-props.map.ts` и доступны пропом `object="cover"` - в вариантах разрешены строго значения с неск классами типа `"bottom-left": "object-bottom object-left"`
- [ ] пересмотреть api архитектуру компонентов `Icon`, `Image`
- [ ] определиться что делать с одинаковыми пропами в одном теге - скорее всего вынести в варианты
- [ ] добиться 100% покрытия тестами 