// NOTE:
// This file is intentionally kept as a compatibility shim.
// UI components import variants from `src/variants/*`, but the source-of-truth is now `src/cdl/*`.
export * from "../cdl/variants";